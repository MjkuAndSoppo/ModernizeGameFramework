# ModernizeGameFramework 更新日志

记录每次改动内容（提交内容、修复内容、修改时间），便于溯源。

---

## [1.4.31] 2026-08-20

### 变更：用 AUI (ApricityUI) 替换战利品 HUD 悬浮窗，移除过滤面板功能

按用户需求（"能否用AUI来替换这个UI"/ 仅 HUD 悬浮窗 / 替换后移除旧UI / 贴合 MGF 现代扁平风 / 取消过滤面板功能），将战利品悬浮窗由原先的 Java 直接绘制替换为 MGF 内嵌 AUI 渲染，并整体移除过滤面板功能。

**AUI 悬浮窗替换：**
- 新建 AUI 模板 `src/main/resources/assets/apricityui/apricity/screens/looting_hud.html`：MGF 现代扁平风（深色半透明面板、圆角、选中高亮、标题栏+数量角标+物品列表+按键提示条）。
- 重写 `overlay/Overlay.java`：改用 `Document.create("screens/looting_hud.html")` + `Base.drawOverlayDocument()` 渲染；物品行通过 `Element.init(document.createElement("item"))` 升级为 `Item` 元素，并以 `setDrivenState()` 驱动物品图标/数量；保留原有激活条件、弹出动画与滚动状态机（`OverlayState`），将结果同步到 AUI 文档（面板定位/缩放、列表可见窗口与内层 translateY 滚动）。
- 移除 `Overlay.java` 中对旧 `OverlayRenderer` / `OverlayLayout` 的渲染调用（这两个类因仍被物品栏列表与配置界面引用而保留）。

**过滤面板功能移除：**
- 删除 `client/filter/FilterPanel.java`、`client/filter/FilterEvents.java`。
- 删除 `mixin/ACSDragMixin.java`（唯一功能为过滤面板拖拽冲突处理），并同步从 `modernizegameframework.mixins.json` 移除其注册。
- 清理 `client/inventory/InventoryLootList.java` 中 `FilterPanel.isOpen()` 判断与相关导入。
- 移除 `KeyInit` 的过滤面板按键（`TOGGLE_FILTER` / `OPEN_FILTER_PANEL`）、`MixinMinecraft` 的过滤键拦截、`KeyTracker` 的过滤模式切换、`Core` 的过滤面板 tick 调用、`BetterLootingClient` 的 `FilterEvents.init()`。
- 保留核心过滤逻辑（`FilterWhitelist` / `FilterBlacklist`），其供 `LootScanner` 扫描时使用，不影响/受过滤面板 UI 影响。

**验证：**
- `gradlew compileJava` 编译通过（仅 `ResourceLocation(String,String)` 过时警告，非错误）。
- `gradlew runClient` 客户端正常启动进入主菜单，AUI 框架正常渲染，无磁盘丢失文档/材质加载报错、无崩溃。悬浮窗视觉表现需在游戏中放置掉落物实测。

---

## [1.4.30] 2026-08-19

### 变更：Better_Looting 全量移植进主模块，成为 MGF 功能

按用户需求（"成为我项目的功能" / 全量移植进主模块），将 [Better_Looting-Architectury](https://github.com/MoHuia/Better_Looting-Architectury) 完整移植进本 Forge 单模块主工程（命名空间改为 `com.modernizegameframework.looting`），并纳入 `/mgf` 命令体系控制。

**移植内容：**
- 整包移植 `com.modernizegameframework.looting`：
  - `config/BetterLootingConfig`：TOML 配置（`run/config/modernizegameframework-looting.toml`），含新增 `enabled` 总开关字段并持久化。
  - `platform/PlatformHooks`：用 `MinecraftForge.EVENT_BUS`（`EntityItemPickupEvent` + `EventPriority.LOWEST` + `receiveCanceled`）实现拾取拦截，兼容"精妙背包"等先拦截的模组。
  - `network/NetworkHandler`：用 Forge `SimpleChannel` 替换 Architectury 网络，含批量拾取/槽位放置/配置同步等数据包。
  - `mixin/ItemEntityMixin` + `ISuperStack`：物品超堆叠；并加入 `enabled` 判断，关闭时恢复原版拾取。
  - 客户端：`BetterLootingClient` / `Core` / `Overlay` / `OverlayRenderer` / `FilterEvents` / `InventoryLootList` / `KeyInit` 全部由 Architectury 事件迁移为 Forge（`TickEvent.ClientTickEvent` / `RenderGuiEvent.Post` / `ScreenEvent` / `InputEvent` / `RegisterKeyMappingsEvent`）。
  - 按键绑定本地化：新增 `assets/better_looting/lang/{zh_cn,en_us}.json`。
- 填充 `src/main/resources/modernizegameframework.mixins.json` 的 common/client mixin 列表。
- `build.gradle` 引入 Mixin 插件及 JEI / Cloth Config / night-config 依赖。
- JEI 兼容做软依赖封装（`JeiCompat`，未装 JEI 不崩溃）。

**命令开关：**
- 新增 `/mgf looting on|off`：切换战利品拾取功能总开关并持久化到配置。
- 更新 `/mgf status` 显示战利品拾取状态。

**修复：**
- 补齐 HUD 九宫格背景纹理 `assets/better_looting/textures/overlay/{vanilla,stardew,terraria}/row(_selected).png`（从 BetterLooting 原始资源复制），修复渲染时 `Failed to load texture: better_looting:textures/overlay/vanilla/row.png / row_selected.png` 报错。
- 补齐 GUI 贴图 `assets/better_looting/textures/gui/panel_bg.png`（过滤/条件面板背景）与 `back_arrow.png`（返回箭头）。
- 修正纹理目录结构：将 3 套皮肤背景归入 `textures/overlay/<skin>/` 子目录（此前复制时丢失 `overlay` 层级，导致 6 个材质加载失败），并补充 GUI 2 个材质，共解决 8 个材质缺失。
- 补齐完整 GUI 中文/英文语言文件（`assets/better_looting/lang/zh_cn.json`、`en_us.json`，约 9.4KB），修复配置/过滤/条件等界面显示裸翻译键名的问题。

**验证：**
- `gradlew runClient` 编译通过、客户端启动进入主菜单稳定运行，日志显示 `Better Looting (MGF) Common Initialized`，无 `Failed to load texture: better_looting:` 材质缺失报错、无崩溃。

**说明：**
- 战利品扫描范围、HUD 位置、拦截策略等可在 `run/config/modernizegameframework-looting.toml` 调整。
- `BetterLooting/` 子目录仍为独立的 Architectury 三平台工程，仅供用户自行对比/微调，不参与主工程构建。

**修改时间：** 2026-08-19

---
## [1.4.29] 2026-08-19

### 变更：拉取 Better_Looting 完整项目源码进入项目目录

按用户要求（自行拉取完整 [Better_Looting-Architectury](https://github.com/MoHuia/Better_Looting-Architectury) 项目微调），将完整三平台源码（common/fabric/forge）拷贝进本项目的 `BetterLooting/` 子目录。

**提交内容：**
- 新增 `e:\project 666\ModernizeGameFramework\BetterLooting\`，包含 `common`、`fabric`、`forge`、`gradle`（wrapper）、`build.gradle`、`gradle.properties`、`gradlew(.bat)`、`settings.gradle`、`.gitignore`。
- 独立于本 Forge 主工程的 Architectury 构建，源码保持不变，便于用户自行微调或构建出 jar。

**说明：**
- 该目录是独立的 Architectury 工程，需将其作为单独工程打开或在其目录下运行 `gradlew` 构建。
- 本 Forge 主工程未引入其依赖，二者互不影响。

**修改时间：** 2026-08-19

---

## [1.4.29] 2026-08-19

### 回退：移除"战利品系统"（Better_Looting 简化移植）

根据用户反馈（部分功能不满足需求），用户将自行拉取完整 [Better_Looting-Architectury](https://github.com/MoHuia/Better_Looting-Architectury) 项目微调，因此移除本次以"核心简化移植"方式集成进本项目的战利品系统。

**移除内容：**
- 删除 `com.modernizegameframework.looting` 包全部 9 个文件（LootingConfig / LootScanner / VisualItemEntry / PacketBatchPickup / PacketPickupRecord / LootPickupManager / LootingNetwork / LootingEvents / LootingClient）。
- 回退 `ModernizeGameFramework.java`：移除 `LootingNetwork`、`LootingConfig` 的注册与导入。
- 回退 `ModCommands.java`：移除 `/mgf looting on|off` 命令及其状态显示。
- 回退 `gradle.properties`：版本号由 1.4.30 还原为 1.4.29。

**说明：**
- 完整 Better_Looting 源码已下载解压在本地：`C:\Users\mjkuj\AppData\Local\Temp\better_looting_unz\Better_Looting-Architectury-1.20.1`，可供自行拉取微调。
- 移除后已重新编译验证通过。

**修改时间：** 2026-08-19