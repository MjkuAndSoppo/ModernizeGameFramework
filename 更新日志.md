# 更新日志

## 2026-07-22

### 版本 1.2.5

**修改时间：** 2026-07-22 04:45

**提交内容：**
- 将模组版本号从 `1.2.4` 迭代至 `1.2.5`。
- 将 5 种安全箱（Alpha/Beta/Gamma/Kappa/Theta）焊入塔科夫背包界面：
  - `TarkovInventoryCapability.MAX_SECURE_SLOTS` 从 4 格扩容至 12 格（兼容 Kappa 3×4），新增 `getSecureCaseType()` 默认方法。
  - `TarkovInventoryCapabilityImpl.secureInventory` 同步扩容至 12 格。
  - `TarkovInventoryMenu.addSecureSlots()` 根据装备的 `SecureContainerType` 动态生成 `cols × rows` 格子，在中部面板居中排列，装备安全箱时显示对应行列内容格。
  - `TarkovInventoryMenu.SecureSlot` 新增禁止放入 `SecureContainerItem` 的检查（安全箱不能放入安全箱）。
  - `TarkovInventoryScreen` 安全箱标签改为居中显示，使用翻译键显示类型名称（如 "Alpha 安全箱"）。
  - `mfg:secure_case` 标签更新为包含 5 种 `SecureContainerItem`。
  - `TarkovInventoryRegistry` 移除旧的 `secure_case` 通用物品，创造标签页改为引用 `SecureContainerRegistry` 的 5 种安全箱。
- 修复左侧装备栏布局：调整顺序为背包(70,57) → 安全箱(70,79) → 副手(70,101)，不再重叠。
- 新增 `NotifyingSlotItemHandler` 内部类：装备槽变更时自动调用 `TarkovInventoryHelper.rebalanceItems()`，卸下/更换胸挂或背包后将锁定物品转移到可用格子或掉落。
- shift+左键快速移动排除副手槽：`quickMoveStack` 的 `equipmentEnd` 改为 `OFFHAND_INDEX`，防止物品优先进入副手。
- 快捷栏 Y 位置改为动态计算：紧跟安全箱区域下方（`secureEndY + 8`），移除固定的 `BASE_HEIGHT` 常量。
- 执行 `./gradlew build` 成功构建，输出新版本 JAR 到 `build\libs\modernizegameframework-1.2.5.jar`。

**修复内容：**
- 修复旧存档 `TarkovInventoryCapabilityImpl.deserializeNBT` 中 `secureInventory.deserializeNBT()` 读取旧存档 `Size: 4` 字段将 handler 缩回 4 格导致 `Slot 4 not in valid range` 崩溃的问题：改为手动逐条读取 `Items` 列表，忽略旧 `Size` 字段。
- 修复 `SecureContainerEvents.onPlayerClone` 同步时机问题：死亡重生后安全箱 overlay 不显示，添加 `SecureContainerNetwork.syncAll()` 调用。
- 修复安全箱无法放入 Curios 自定义槽位：新增 `data/curios/tags/items/secure_container.json` 标签文件，包含全部 5 种安全箱。

---

## 2026-07-22

### 版本 1.2.4

**修改时间：** 2026-07-22 04:12

**提交内容：**
- 将模组版本号从 `1.2.3` 迭代至 `1.2.4`。
- 完成 `./gradlew build` 构建并运行客户端验证：游戏正常加载，玩家可进入世界，未出现崩溃或异常。
- 实现塔科夫背包全屏布局：`TarkovInventoryScreen.init` 将 `imageWidth` / `imageHeight` 设为屏幕尺寸，并固定 `leftPos` / `topPos` 为 0，使界面横向占满整个 Minecraft 窗口，保留左侧装备区、中段主仓库/扩展格、右侧容器区的三段式结构。
- 实现安全箱功能：
  - `TarkovInventoryCapability` 接口新增 `SLOT_SECURE_CASE`、`MAX_SECURE_SLOTS`、`getSecureCase` / `setSecureCase` / `getSecureInventory`。
  - `TarkovInventoryCapabilityImpl` 扩展装备槽至 3 个，新增 2×2 安全箱内容格，序列化/反序列化时保存 `Secure` 数据。
  - `TarkovInventoryMenu` 在装备安全箱时添加 4 个 `SecureSlot`，未装备时不添加内容格（直接隐藏）。
  - `TarkovInventoryMenu` 新增 `hasSecureSlots` 标志，修正未装备时 `isSecureSlot` / `isLockedSlot` 对主仓库前 4 格的误判定。
  - `TarkovInventoryCapabilityRegistry` 在玩家死亡/克隆时保留安全箱装备槽与内容格，仅扩展格物品通过 `LivingDropsEvent` 掉落。
  - `TarkovInventoryRegistry` 注册 `secure_case` 物品并加入装备创造标签页。
  - 新增 `data/mfg/tags/items/secure_case.json` 标签文件，限制只有安全箱物品可放入安全箱装备槽。
- 简化 `OpenInventoryPacket` 为无参包，修复 `TarkovInventoryEvents.onGuiOpen` 空参调用导致的编译错误；`TarkovInventoryMenuProvider` 新增 `extraDataWriter` 静态方法供 `NetworkHooks.openScreen` 使用。
- 执行 `./gradlew build` 成功构建，输出新版本 JAR 到 `build\libs\modernizegameframework-1.2.4.jar`。

**修复内容：**
- 修复 `OpenInventoryPacket` 构造函数与调用方参数不匹配导致的编译失败。
- 修复 `TarkovInventoryScreen` 中直接修改 `Slot.x` / `Slot.y` 失败的问题（`Slot` 字段为 final），改为在 `TarkovInventoryMenu` 中根据安全箱装备状态动态决定是否添加槽位，实现隐藏效果。
- 修复按 E 无法打开背包的问题：旧存档反序列化后 `Equipment` ItemStackHandler 只有 2 个槽位（胸挂/背包），访问新增的安全箱槽位 2 时越界。`TarkovInventoryCapabilityImpl.deserializeNBT` 改为手动读取装备槽数据，兼容旧存档并在读取前清空为 3 个槽位。
- 将 `ModernizeGameFramework.LOGGER` 访问权限改为 `public`，便于各模块统一记录日志。

---

## 2026-07-22

### 版本 1.2.3

**修改时间：** 2026-07-22 00:12

**提交内容：**
- 将模组版本号从 `1.2.2` 迭代至 `1.2.3`。
- 在 `TarkovInventoryScreen` 新增 `renderSlotOutlines` 方法，为每个物品槽绘制自定义灰色边框线。

**修复内容：**
- 修复塔科夫背包快捷键（Alt+点击移到装备区、Ctrl+点击移到容器区）移动物品后客户端显示位置错乱的问题：在 `TarkovInventoryQuickMove.moveToEquipment` 和 `moveToContainer` 末尾调用 `menu.broadcastChanges()`，确保服务端修改后立即同步到客户端。

---

## 2026-07-21

### 版本 1.2.2

**修改时间：** 2026-07-21 21:10

**提交内容：**
- 将模组版本号从 `1.2.1` 迭代至 `1.2.2`。
- 新增 `TarkovInventoryScreen` 自定义三段式背包界面，包含顶部标签页（装备/医疗/技能，后两者点击提示尚未开放）、左侧装备区、中段主仓库/扩展格、右侧容器区（打开箱子时显示）。
- 新增 `TarkovInventoryEvents` 事件监听器，客户端通过 `ScreenEvent.Opening` 拦截原版 `InventoryScreen`，服务端通过 `PlayerInteractEvent.RightClickBlock` 拦截容器方块打开并替换为塔科夫背包界面。
- 新增 `TarkovInventoryNetwork` 网络通道，实现客户端请求打开背包、装备槽与扩展格数据同步、Alt/Ctrl 快捷键移动网络包。
- 新增 `TarkovInventoryQuickMove` 服务端快捷键移动逻辑：Alt+点击移到装备区（胸挂/背包/护甲/副手），Ctrl+点击移到右侧容器区。
- 在 `ModernizeGameFramework` 主类中注册塔科夫背包的物品、菜单类型、创造标签页、网络消息与客户端 Screen。
- 为 `TarkovInventoryCapability` 接口及实现补充 `getEquipmentInventory()` 方法，供装备槽读取。
- 更新 `zh_cn.json` / `en_us.json` 语言文件，新增标签页“尚未开放”提示文本。

**修复内容：**
- 修复 `TarkovInventoryMenu` 中 `TransientCraftingContainer` 错误使用玩家背包列表的问题，改为独立 2x2 合成格列表，避免污染玩家背包。
- 修复 `TarkovInventoryHelper.tryPlaceInOpenSlots` 中 lambda 引用非最终局部变量导致的编译错误。
- 修复 `TarkovInventoryScreen` 中错误重写私有 `renderSlot` 方法的问题，改为在 `render` 流程结束后统一绘制锁定格遮罩。

---

## 2026-07-21

### 版本 1.2.2

**修改时间：** 2026-07-21 23:45

**提交内容：**
- 无新增功能。

**修复内容：**
- 修复创造模式背包被错误替换为塔科夫背包的问题：在 `TarkovInventoryEvents.onGuiOpen` 中排除 `CreativeModeInventoryScreen`，仅替换生存模式原版背包。

---

## 2026-07-21

### 版本 1.2.2

**修改时间：** 2026-07-21 23:55

**提交内容：**
- 无新增功能。

**修复内容：**
- 进一步加强创造模式背包保护：在 `TarkovInventoryEvents.onGuiOpen` 中增加玩家 `isCreative()` 判断，确保创造模式下任何背包界面都不会被替换为塔科夫背包。

---

## 2026-07-21

### 版本 1.2.1

**提交内容：**
- 将模组版本号从 `1.2.0` 迭代至 `1.2.1`，避免覆盖已存在的构建产物。
- 执行 `./gradlew build` 成功构建，输出新版本 JAR 到 `build\libs\modernizegameframework-1.2.1.jar`。

**修复内容：**
- 无（本次为版本输出，未修复具体功能问题）。

---
