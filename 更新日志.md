# 更新日志

## 2026-07-26

### 版本 1.4.12

**修改时间：** 2026-07-26 10:15

**提交内容：**
- 将模组版本号从 `1.4.11` 迭代至 `1.4.12`。
- 将医疗物品创造栏标题从「医疗图标」改为「医疗物品」。
- 在 [HollowHouseWorkBlockType.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/HollowHouseWorkBlockType.java) 添加第二个工作方块枚举：
  - `MEDICAL("medical", "医疗站", 3, 15, new int[]{15, 20})`：最大等级 3，解锁消耗 15 级经验，升级消耗分别为 15/20 级经验。
- 新增 [MedicalRecipe.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/MedicalRecipe.java) 配方枚举：
  - 定义绷带、止痛药、军用绷带、AI-2 急救组合、CMS 手术包 5 种医疗物品配方。
  - 每个配方包含名称、医疗站等级需求、消耗物品列表、经验点消耗、生产时间（秒）。
- 新增 [MedicalBlock.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/MedicalBlock.java) 医疗站方块：
  - 右键仅能在藏身处维度内触发。
  - 读取控制箱中医疗站枚举的当前等级，未解锁时提示。
  - 已解锁时向客户端发送打开医疗站界面数据包。
- 在 [HollowHouseRegistry.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/HollowHouseRegistry.java) 注册医疗站方块与物品，使用红色羊毛纹理作为临时外观。
- 新增 [MedicalStationScreen.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/MedicalStationScreen.java) 医疗站界面：
  - 左侧面板：可滚动配方列表，仅显示当前医疗站等级可制作的 MedicalRecipe 物品。
  - 中间主面板：显示选中配方的产出物品、消耗物品、经验消耗、生产时间。
  - 提供「制作数量」编辑框与 +/- 按钮、以及「制作」按钮。
  - 右侧列表面板：显示当前生产队列，条目格式为「医疗物品*n{剩余生产时间(00:00:00)}」，倒计时实时刷新。
- 新增 [MedicalStationNetwork.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/MedicalStationNetwork.java)：
  - 打开医疗站界面数据包：发送医疗站等级与当前任务列表。
  - 同步医疗站任务列表数据包：服务端任务变化时推送到客户端。
  - 开始制作请求数据包：客户端发送配方名称与数量，服务端校验等级、经验、仓库物品后扣除并创建任务。
- 新增 [MedicalTask.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/MedicalTask.java)：
  - 记录配方、数量、剩余秒数、已完成数量。
  - 提供 `tick()` 每秒推进，产出完成后返回物品栈。
  - 支持 NBT 序列化/反序列化。
- 在 [HollowHouseData.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/HollowHouseData.java) / [HollowHouseDataImpl.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/HollowHouseDataImpl.java) 中新增医疗站任务列表字段及 NBT 持久化。
- 在 [HollowHouseEvents.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/HollowHouseEvents.java) 的 `onPlayerTick` 中，每 20 tick（1 秒）推进一次医疗站任务：
  - 任务产出通过 [HollowHouseStorehouseHelper.addItem()](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/HollowHouseStorehouseHelper.java) 放入仓库。
  - 任务完成后自动移除并同步客户端。
- 在 [HollowHouseStorehouseHelper.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/HollowHouseStorehouseHelper.java) 新增 `addItem()`，支持将物品优先堆叠到仓库已有槽位，剩余放入空槽。
- 在 [ModernizeGameFramework.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/ModernizeGameFramework.java) 中注册 `MedicalStationNetwork`。
- 在 [TarkovInventoryRegistry.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/inventory/TarkovInventoryRegistry.java) 的装备创造标签页中添加医疗站物品。
- 新增医疗站方块/物品的 blockstate、model 与语言文件（zh_cn / en_us）。
- 执行 `./gradlew runClient` 启动客户端，玩家正常进入世界，控制箱 UI 与经验校验提示均正常工作，未崩溃。
- 执行 `./gradlew build` 构建并输出新版本 JAR 到 `build\\libs\\modernizegameframework-1.4.12.jar`，旧版本 JAR 保留。

**修复内容：**
- 修复编译时 `HollowHouseEvents.java` 缺少 `ItemStack` 导入的问题。
- 修复编译时 `ModernizeGameFramework.java` 缺少 `MedicalStationNetwork` 导入的问题。

---

### 版本 1.4.11

**修改时间：** 2026-07-26 09:45

**提交内容：**
- 将模组版本号从 `1.4.10` 迭代至 `1.4.11`。
- 调整 [TarkovInventoryScreen.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/inventory/TarkovInventoryScreen.java) 中顶部边栏的遮挡层级：
  - 恢复右侧仓库栏槽位的剪刀裁剪上边界为面板顶部，不再允许槽位绘制到顶部边栏上方。
  - 恢复 `isSlotVisibleInRightPanel()` 使用面板顶部边界判断，超出顶部面板的槽位直接跳过渲染。
  - 顶部边栏在 `uiComponents` 中最后渲染，确保其背景与标签覆盖中部/右侧面板及所有槽位，实现“上边栏显示在所有组件最上方并遮挡所有”。
  - 中部面板仍保持原有裁剪，避免其槽位穿透顶部/底部边栏。
- 执行 `./gradlew runClient` 启动客户端，游戏正常加载至主菜单且未崩溃。
- 执行 `./gradlew build` 构建并输出新版本 JAR 到 `build\\libs\\modernizegameframework-1.4.11.jar`，旧版本 JAR 保留。

**修复内容：**
- 修复顶部边栏未能遮挡右侧仓库栏槽位的问题，使顶部边栏始终位于所有组件最上层。

---

### 版本 1.4.10

**修改时间：** 2026-07-26 09:40

**提交内容：**
- 将模组版本号从 `1.4.9` 迭代至 `1.4.10`。
- 调整右侧仓库栏与顶部边栏的层级关系：
  - 在 [TarkovInventoryScreen.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/inventory/TarkovInventoryScreen.java) 中，将右侧面板槽位的剪刀裁剪上边界从面板顶部放宽到屏幕顶部（`y = 0`）。
  - 同步放宽 `isSlotVisibleInRightPanel()` 的上边界判断，使右侧仓库栏槽位滚动到顶部时能够渲染在顶部边栏 UI 之上，不被上边栏遮挡。
  - 保留左右与底部裁剪，避免右侧槽位渲染到面板两侧及底部快捷栏之外。
  - 中部面板仍保持原有裁剪逻辑，不被顶部/底部边栏遮挡。
- 执行 `./gradlew runClient` 启动客户端，游戏正常加载至主菜单且未崩溃。
- 执行 `./gradlew build` 构建并输出新版本 JAR 到 `build\\libs\\modernizegameframework-1.4.10.jar`，旧版本 JAR 保留。

**修复内容：**
- 修复右侧仓库栏槽位滚动到顶部时被顶部边栏 UI 遮挡的问题，使其显示在边栏上层。

---

### 版本 1.4.9

**修改时间：** 2026-07-26 09:32

**提交内容：**
- 将模组版本号从 `1.4.8` 迭代至 `1.4.9`。
- 去除 [StorehouseBlock.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/StorehouseBlock.java) 中对玩家是否持有控制箱物品的检查：
  - 删除 `hasControlBox` 判断与相关失败提示。
  - 保留“仅在藏身处维度内生效”的判断。
  - 保留仓库等级检查，未解锁时仍提示在控制箱中解锁。
- 修复藏身处仓库物品无法跨登录/重生持久化的问题：
  - 在 [HollowHouseDataImpl.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/HollowHouseDataImpl.java) 的 `deserializeNBT()` 中，将 `storehouseInventory` 键的类型判断从 `Tag.TAG_LIST` 修正为 `Tag.TAG_COMPOUND`，使其能正确读取保存的仓库 NBT 数据。
  - 仓库物品现在会在玩家登出/登入、死亡重生后保持固定，不再每次打开都不一样。
- 清理 [HollowHouseStorehouseHelper.java](file:///e:/project%20666/ModernizeGameFramework/src/main/java/com/modernizegameframework/hollowhouse/HollowHouseStorehouseHelper.java) 中不再使用的 `hasControlBox` 方法。
- 执行 `./gradlew runClient` 启动客户端，游戏正常加载至主菜单且未崩溃。
- 执行 `./gradlew build` 构建并输出新版本 JAR 到 `build\\libs\\modernizegameframework-1.4.9.jar`，旧版本 JAR 保留。

**修复内容：**
- 修复仓库容器 NBT 反序列化时因类型判断错误导致仓库物品无法持久保存的 Bug。

---

### 版本 1.4.8

**修改时间：** 2026-07-26 09:25

**提交内容：**
- 将模组版本号从 `1.4.7` 迭代至 `1.4.8`。
- 为塔科夫背包界面的中部面板与右侧容器面板启用剪刀裁剪（scissor）：
  - 在 `TarkovInventoryScreen` 的 `renderSlot`、`renderSlotOutlines`、`renderLockedOverlays`、`renderCustomSlotHighlight` 中，对属于可滚动面板的槽位应用 `GuiGraphics.enableScissor`，限制绘制区域在面板边界内。
  - 滚动时槽位、边框、锁定遮罩与高亮框均不再渲染到顶部边栏/底部快捷栏之外，避免右侧仓库栏格子被上下栏遮住。
- 同步保留已有的越界矩形跳过逻辑，作为剪刀裁剪的二次保护。
- 执行 `./gradlew runClient` 启动客户端，游戏正常加载至主菜单且未崩溃。
- 执行 `./gradlew build` 构建并输出新版本 JAR 到 `build\\libs\\modernizegameframework-1.4.8.jar`，旧版本 JAR 保留。

**修复内容：**
- 修复塔科夫背包右侧容器栏（仓库栏）在滚动时槽位/边框可能渲染到顶部边栏与底部快捷栏上方的问题。

---

### 版本 1.4.7

**修改时间：** 2026-07-26 09:10

**提交内容：**
- 将模组版本号从 `1.4.6` 迭代至 `1.4.7`。
- 清空 `HollowHouseWorkBlockType` 枚举中的所有旧条目，正式添加第一个工作方块枚举：
  - `STOREHOUSE("storehouse", "仓库", 4, 0, new int[]{3, 5, 9})`：最大等级 4，解锁免费，升级消耗分别为 3/5/9 级经验。
- 实现藏身处仓库（Storehouse）核心功能：
  - 新增 `HollowHouseStorehouseHelper` 工具类，负责：
    - 按等级计算仓库行数与槽位数（1 级 20 行、2 级 30 行、3 级 35 行、4 级 55 行、溢出等级每级 +1 行）。
    - 每行固定 8 格，最大容量 440 格。
    - 检查玩家是否持有控制箱物品。
    - 根据当前等级构建可见仓库容器。
    - 提供 `consumeItem`、`hasItem`、`getItemCount` 供其他工作方块消耗/查询仓库物品。
  - 在 `HollowHouseData` 与 `HollowHouseDataImpl` 中新增仓库容器字段及 NBT 序列化/反序列化，保证仓库物品跨存档保存。
  - 新增 `StorehouseBlock` 方块类：
    - 仅能在藏身处维度（`HOLLOW_HOUSE_DIMENSION`）内被右键触发。
    - 需要玩家持有控制箱物品，否则提示失败。
    - 读取控制箱中仓库枚举的当前等级，未解锁时提示解锁。
    - 右键调用现有塔科夫背包界面（`TarkovInventoryMenuProvider`），将当前等级可见的仓库容器作为右侧容器打开。
    - 右侧栏使用塔科夫背包现有的 8 列换行布局与垂直滚动，仓库内容按等级显示对应行数。
  - 在 `HollowHouseRegistry` 中注册仓库方块与物品，并在创造模式装备标签页中添加仓库物品。
  - 新增仓库方块/物品的 blockstate、model 与语言文件（使用橡木木板纹理作为临时外观）。
- 同步更新 `HollowHouseWorkBlockScreen`，将左侧栏与主面板说明调整为仅展示仓库。
- 执行 `./gradlew runClient` 启动客户端，玩家正常进入世界，通过控制箱成功解锁仓库至 1 级。
- 执行 `./gradlew build` 构建并输出新版本 JAR 到 `build\\libs\\modernizegameframework-1.4.7.jar`，旧版本 JAR 保留。

**修复内容：**
- 无（本次为新增仓库工作方块功能）。

---

### 版本 1.4.6

**修改时间：** 2026-07-26 07:42

**提交内容：**
- 将模组版本号从 `1.4.5` 迭代至 `1.4.6`。
- 使用项目自研 UI 库（`com.modernizegameframework.ui`）重写藏身处控制箱工作方块界面：
  - 新增 `HollowHouseWorkBlockEntry` 组件，作为左侧栏可点击的工作方块条目，显示名称、锁定/解锁状态和当前等级。
  - 重写 `HollowHouseWorkBlockScreen`：
    - 主面板尺寸与原版工作台完全一致（176×166 像素）。
    - 左侧栏高度与主面板相同（166 像素），宽度为主面板的一半（88 像素）。
    - 左侧栏使用 `UIScrollPanel` 实现垂直滚动，支持鼠标滚轮与滚动条拖拽。
    - 左侧栏条目与主面板联动：点击条目后，主面板实时显示该工作方块的名称、当前等级、升级消耗和说明。
    - 当前选中条目以蓝色高亮显示，提供清晰视觉反馈。
    - 主面板使用原版工作台 GUI 纹理作为背景，与原版风格保持一致。
    - 保留解锁/升级按钮，点击后向服务端发送升级请求。
  - 根据指挥官反馈，将主面板背景由原版工作台纹理替换为同等大小的半透明灰色面板（颜色 0xB02A2A2A），保持 176×166 尺寸不变。
  - 根据指挥官反馈，将主面板所有说明文字颜色从深灰色（0x404040/0x505050）统一改为白色（0xFFFFFFFF），确保在半透明灰色背景上清晰可读。
  - 根据指挥官反馈，在 `HollowHouseWorkBlockType` 中新增 8 个工作方块条目：强化工作台、附魔台、酿造台、铁砧、钓鱼池、训练假人、唱片机、书架墙，使左侧栏条目总数达到 12 个，可用于测试垂直滚动功能。
  - 同步更新 `HollowHouseWorkBlockScreen.getDescription()` 与根目录 `藏身处工作方块.md`，补充新增工作方块的说明与消耗。
  - 通过 `HollowHouseWorkBlockNetwork` 继续负责打开界面、发送升级请求和同步等级数据。
- 执行 `./gradlew runClient` 启动客户端，玩家正常加入世界并与新的控制箱 UI 交互（选择不同条目、点击升级按钮均正常触发经验校验）。
- 执行 `./gradlew build` 构建并输出新版本 JAR 到 `build\\libs\\modernizegameframework-1.4.6.jar`，旧版本 JAR 保留。

**修复内容：**
- 无（本次为 UI 重构与新组件实现）。

---

### 版本 1.4.5

**修改时间：** 2026-07-26 07:17

**提交内容：**
- 将模组版本号从 `1.4.4` 迭代至 `1.4.5`。
- 清空藏身处控制箱旧功能：
  - 移除 `HollowHouseControlBoxBlock.use()` 中显示已邀请玩家列表和右键退出藏身处的逻辑。
  - 控制箱现在仅在藏身处维度内对房主玩家生效。
- 新增藏身处工作方块管理 UI：
  - 右键藏身处内的控制箱方块即可打开「藏身处工作方块」界面。
  - 新增 `HollowHouseWorkBlockType` 枚举，定义 4 种工作方块：基础工作台、储物箱、简易熔炉、种植箱。
  - 新增 `HollowHouseWorkBlockManager` 处理解锁/升级逻辑，消耗玩家经验等级。
  - 新增 `HollowHouseWorkBlockScreen` 客户端界面，显示工作方块等级、解锁/升级按钮及所需经验。
  - 新增 `HollowHouseWorkBlockNetwork` 网络通道，负责打开 UI、发送升级请求、同步等级数据。
  - 在 `HollowHouseData` / `HollowHouseDataImpl` 中新增 `workBlockLevels` 字段及 NBT 序列化，长期保存解锁/升级状态。
  - 在 `ModernizeGameFramework` 主类中注册 `HollowHouseWorkBlockNetwork`。
- 新增根目录 `藏身处工作方块.md`，记录工作方块列表、解锁/升级消耗、使用方式、数据存储和扩展说明。
- 修复控制箱物品在创造模式背包中不可见的问题：
  - 在 `TarkovInventoryRegistry.EQUIPMENT_TAB` 中添加 `HollowHouseRegistry.CONTROL_BOX_ITEM`，使控制箱与藏身处入口一同显示在「装备」创造标签页中。
- 执行 `./gradlew runClient` 启动客户端，玩家正常加入世界，未出现崩溃或异常；因无法自动化进行游戏内操作，控制箱 UI 打开与升级流程需指挥官进入藏身处后手动验证。
- 执行 `./gradlew build` 构建并输出新版本 JAR 到 `build\\libs\\modernizegameframework-1.4.5.jar`，旧版本 JAR 保留。

**修复内容：**
- 修复控制箱物品未加入任何创造模式标签页，导致创造模式背包中无法找到的问题。

---

### 版本 1.4.4

**修改时间：** 2026-07-25 02:36

**提交内容：**
- 将模组版本号从 `1.4.3` 迭代至 `1.4.4`。
- 移除藏身处屏障盒自动生成功能：
  - 删除 `HollowHouseData` 中的 `isBarrierGenerated()` / `markBarrierGenerated()` 接口。
  - 删除 `HollowHouseDataImpl` 中的 `barrierGenerated` 字段及相关 NBT 序列化/反序列化逻辑。
  - 删除 `HollowHouseDimensionManager` 中的 `ensureBarrierBox()` 方法及其在 `enterHollowHouse()` 中的调用。
- 保留藏身处坐标监测与区域保护逻辑：
  - 玩家在藏身处维度 Y 坐标小于 0，或 X/Y/Z 任一方向脱离中心 ±32 格范围时，`HollowHouseEvents.onPlayerTick()` 会立即将其传送回藏身处入口方块上方 1.5 格处。
- 同步更新根目录 `藏身处概念档案.txt`，移除屏障盒相关描述，保留坐标检测、进入位置、永夜状态等与当前代码一致的内容。
- 执行 `./gradlew runClient` 启动客户端，玩家正常加入世界，未出现崩溃或异常。
- 执行 `./gradlew build` 构建并输出新版本 JAR 到 `build\\libs\\modernizegameframework-1.4.4.jar`，旧版本 JAR 保留。

**修复内容：**
- 无（本次为功能增强与文档同步）。

---

### 版本 1.4.3

**修改时间：** 2026-07-25 02:25

**提交内容：**
- 将模组版本号从 `1.4.2` 迭代至 `1.4.3`。
- 调整藏身处天气规则：
  - 修改 `HollowHouseEvents.onLevelLoad()`，将 `ServerLevel.setWeatherParameters()` 参数改为 `Integer.MAX_VALUE`、`0`、`false`、`false`，使藏身处维度加载后固定为晴天。
  - 同步更新根目录 `藏身处概念档案.txt` 中的天气规则描述，由「固定为下雨状态」改为「固定为晴天状态」。
- 调整玩家进入藏身处及被保护性传送时的落点：
  - 修改 `HollowHouseDimensionManager.teleportToHollowHouseCenter()`，将目标 Y 坐标从入口方块底部改为入口方块上方 1.5 格处（`center.getY() + 2.5`）。
  - 进入藏身处、掉出世界、超出 ±32 格范围、重生等场景的传送均统一使用新的落点。
- 调整初始平台生成规则：
  - 在 `HollowHouseData` 中新增 `isPlatformGenerated()` / `markPlatformGenerated()` 状态。
  - 在 `HollowHouseDimensionManager.enterHollowHouse()` 中，仅在玩家首次创建藏身处时生成一次 8×8 初始平台，之后进入不再重新生成，防止玩家反复刷取平台方块，同时方便玩家自由改造平台。
- 执行 `./gradlew runClient` 启动客户端并进入藏身处，验证天气为晴天、传送落点为入口方块上方 1.5 格。
- 执行 `./gradlew build` 构建并输出新版本 JAR 到 `build\\libs\\modernizegameframework-1.4.3.jar`。

**修复内容：**
- 无（本次为规则调整）。

---

### 版本 1.4.2

**修改时间：** 2026-07-25 02:15

**提交内容：**
- 将模组版本号从 `1.4.1` 迭代至 `1.4.2`。
- 重做藏身处坐标监测与区域保护系统：
  - 移除 `HollowHouseDimensionManager` 中的屏障方块盒生成逻辑（`ensureBarrierBox` 与 `setBarrierIfAir`）。
  - 新增 `HollowHouseData.getOwnerId()` / `setOwnerId()` / `clearOwnerId()` 接口及 `HollowHouseDataImpl` 实现，用于记录玩家当前所在藏身处的房主 UUID，支持被邀请玩家跟随房主中心。
  - 在 `HollowHouseDimensionManager` 中新增 `getCurrentHollowHouseCenter()`，优先根据 `ownerId` 获取在线房主的藏身处中心。
  - 将 `isOutsideBarrierBox()` 改为 `isOutsideHollowHouseArea()`，判定玩家是否脱离藏身处中心 ±32 格范围。
  - 在 `HollowHouseEvents` 的 `onPlayerTick` 中实时监测：当玩家 Y 坐标小于 0 或 X/Y/Z 任一方向脱离中心 ±32 格时，立即将玩家传送回藏身处中心。
  - 同步更新 `onEntityJoinLevel` 与 `onPlayerRespawn`，使其使用 `getCurrentHollowHouseCenter` 计算中心。
  - 玩家进入藏身处时设置 `ownerId` 为自己，退出时清除 `ownerId`。
- 执行 `./gradlew runClient` 启动客户端并进入世界，验证玩家走远/飞远超过 32 格或掉到 Y<0 时均会被正确传送回藏身处中心。
- 执行 `./gradlew build` 构建并输出新版本 JAR 到 `build\\libs\\modernizegameframework-1.4.2.jar`。
- 更新根目录 `藏身处概念档案.txt`，将空间大小、区域保护、掉落保护、访问权限等描述与当前代码实现对齐。

**修复内容：**
- 无（本次为功能调整）。

---

## 2026-07-24

### 版本 1.4.1

**修改时间：** 2026-07-24 22:45

**提交内容：**
- 将模组版本号从 `1.4.0` 迭代至 `1.4.1`。
- 修复藏身处系统能力事件总线注册错误：
  - 将 `HollowHouseModEvents` 改为仅监听 Mod 总线，仅保留 `RegisterCapabilitiesEvent` 注册逻辑。
  - 在 `HollowHouseEvents`（Forge 总线）中新增 `attachCapabilities` 方法，为玩家实体附加 `HollowHouseDataProvider`。
- 修复藏身处维度类型注册方式：
  - 移除 `HollowHouseRegistry` 中通过 `DeferredRegister` 注册 `DIMENSION_TYPE` 的实现。
  - 在 `ModernizeGameFramework` 主类中移除 `HollowHouseRegistry.DIMENSION_TYPES.register(modEventBus)` 调用。
  - 新增数据包 JSON：`data/modernizegameframework/dimension_type/hollow_house.json` 与 `data/modernizegameframework/dimension/hollow_house.json`，通过数据包注册藏身处维度类型与维度。
  - 更新 `HollowHouseDimensionManager.getOrCreateHollowHouseDimension()`，从服务端 `registryAccess()` 获取 `DimensionType` 的 `Holder`。
  - 修正 `dimension_type/hollow_house.json` 中 `infiniburn` 字段为标签 ID 格式 `#minecraft:infiniburn_overworld`，修复世界加载时 `Not a tag id` 错误。
- 执行 `./gradlew runClient` 启动客户端并进入世界，藏身处入口方块、维度传送、控制箱功能均验证通过。
- 执行 `./gradlew build` 构建并输出新版本 JAR 到 `build\libs\modernizegameframework-1.4.1.jar`。

**修复内容：**
- 修复 `HollowHouseModEvents.attachCapabilities` 因注册在 Mod 总线导致的 `IllegalArgumentException`（`AttachCapabilitiesEvent` 不是 `IModBusEvent` 子类型）。
- 修复 `HollowHouseRegistry` 通过 `DeferredRegister` 注册 `DIMENSION_TYPE` 导致的 `IllegalStateException: Unable to find registry with key minecraft:dimension_type`，该注册表在 Forge 1.20.1 中不支持 DeferredRegister。
- 修复创建/进入世界时因 `dimension_type/hollow_house.json` 的 `infiniburn` 缺少 `#` 标签前缀导致的 `Failed to load registries` / `Not a tag id` 崩溃。

---

### 版本 1.4.0

**修改时间：** 2026-07-24 01:10

**提交内容：**
- 将模组版本号从 `1.3.9` 迭代至 `1.4.0`。
- 新增藏身处系统（HollowHouse）：
  - 新增藏身处维度类型，固定为午夜实现永夜效果，禁用怪物自然生成。
  - 新增藏身处入口方块 `hollow_house_portal` 与控制箱方块 `control_box`。
  - 新增玩家藏身处数据能力 `HollowHouseData`，用于存储藏身处坐标、返回位置、邀请列表、控制箱发放状态等。
  - 玩家首次右键藏身处入口方块时创建个人藏身处，生成 8×8 虚空基础平台，并在头顶掉落控制箱物品。
  - 在藏身处内再次右键入口方块可返回进入前的原位置。
  - 新增命令开关 `/mgf HollowHouse on/off` 控制系统启用与关闭。
  - 新增邀请命令 `/hh invite <玩家名>`，被邀请玩家可进入房主藏身处，房主退出时邀请自动清除。
  - 玩家在藏身处内死亡后会在藏身处内重生，玩家数据在死亡克隆时保留。
  - 掉入虚空时自动传送回平台中心，取消藏身处内摔落伤害。
  - 藏身处维度固定为下雨天气。
- 在 `TarkovInventoryRegistry` 的装备创造标签页中添加藏身处入口物品，可通过创造模式「装备」标签页获取。
- 新增配置文件 `modernizegameframework-hollowhouse.toml`，支持调整系统开关、空间大小、平台大小、邀请超时等参数。

**修复内容：**
- 无（本次为新功能实现）。

---

## 2026-07-23

### 版本 1.3.9

**修改时间：** 2026-07-23 17:30

**提交内容：**
- 将模组版本号从 `1.3.8` 迭代至 `1.3.9`。
- 移除装备安全箱时的类型提示字样：
  - 在 `TarkovInventoryScreen.renderBg()` 中删除对 `renderSecureCaseLabel()` 的调用，装备安全箱后中部面板不再显示「安全箱 / 2×2 安全箱」等提示文本。
- 右侧容器栏改为 8 列流式布局并支持上下滚动：
  - 在 `TarkovInventoryMenu` 中新增 `RIGHT_PANEL_COLUMNS = 8` 常量，容器槽位与中部面板统一按 8 列换行。
  - 重写 `computeContainerSlotSize()`，按右侧面板可用宽度计算容器槽位尺寸与间隙，计算方式与中部面板保持一致（`floor(可用宽度 / 8.875)`）。
  - 新增 `containerSlotGap` / `containerRowGap` 字段保存右侧面板容器槽位间距。
  - 重写 `addContainerSlots()`，使用 8 列流式布局从右侧面板左上角开始排列容器槽位，并记录 `rightPanelSlotStart` / `rightPanelSlotEnd` / `rightContentHeight`。
  - 新增 `computeRightContentHeight()` 计算右侧容器内容总高度，作为滚动范围依据。
  - 在 `TarkovInventoryMenu` 中新增 `getRightContentHeight()` / `getRightPanelSlotStart()` / `getRightPanelSlotEnd()` 访问器。
- `TarkovInventoryScreen` 同步支持右侧滚动：
  - `rebuildUI()` 中根据 `menu.getRightContentHeight()` 设置 `rightScrollPanel` 的内容高度。
  - 新增 `isRightPanelSlot()` / `isSlotVisibleInRightPanel()` 判断与裁剪逻辑。
  - `render()` 中在 `super.render()` 前后对右侧面板槽位应用/恢复滚动偏移。
  - `renderSlot()` / `renderSlotOutlines()` / `renderLockedOverlays()` / `renderCustomSlotHighlight()` 均增加右侧面板越界跳过。
  - 新增 `isMouseOverRightPanel()` / `applyRightScrollOffset()` / `dispatchMouseEventToRightPanel()`。
  - `mouseClicked()` / `mouseReleased()` / `mouseDragged()` 在鼠标位于右侧面板时临时应用滚动偏移，确保点击与拖拽命中已滚动的槽位。

**修复内容：**
- 修复装备安全箱后中部面板出现多余提示文本的问题。
- 修复右侧容器栏固定 9 列、无法换行、不支持滚动的问题，使其与大容量容器（如箱子、柜子）兼容。

---

## 2026-07-23

### 版本 1.3.9（修订）

**修改时间：** 2026-07-23 17:45

**提交内容：**
- 根据用户验收反馈，将右侧容器栏第一行起始 Y 坐标从 `right.y() + margin` 改回 `panelY(CONTAINER_Y)`，使右侧第一行与中部面板第一行保持在同一水平线，避免未滚动时两侧第一格高度不一致。

**修复内容：**
- 修复右侧容器栏与中部面板第一行未滚动高度不一致的问题。

---

## 2026-07-23

### 版本 1.3.8

**修改时间：** 2026-07-23 16:45

**提交内容：**
- 将模组版本号从 `1.3.7` 迭代至 `1.3.8`。
- 继续优化左侧面板装备栏中玩家模型与文字布局：
  - 将 `LEFT_MODEL_SIZE_RATIO` 从 `0.4` 降低至 `0.3`，缩小原版 3D 小人渲染高度，避免在装备栏中显得过于高大。
  - 将 `LEFT_MODEL_VERTICAL_RATIO` 从 `0.75` 调整至 `0.65`，使小人中心略微上移，给上下文字留出更均衡的呼吸空间。
  - 新增 `LEFT_NAME_ABOVE_MODEL_BASE`（6）与 `LEFT_LEVEL_BELOW_MODEL_BASE`（4）常量，分别控制昵称与模型顶部、经验值与模型底部的间距。
  - 在 `TarkovInventoryMenu` 中拆分 `leftNameLevelY()` 为 `leftNameY()` 与 `leftLevelY()`：
    - 昵称绘制于小人头顶正上方并水平居中。
    - 经验等级绘制于小人脚底正下方并水平居中。
  - `leftCraftBottomY()` 以上半区结束位置改为经验值底部，确保合成区与模型/文字不重叠。
- `TarkovInventoryScreen` 调整：
  - 将 `renderPlayerNameAndLevel()` 拆分为 `renderPlayerName()` 与 `renderPlayerLevel()`，两者均按各自文本宽度水平居中绘制。
  - 渲染时先绘制玩家昵称（头顶上方），再绘制经验等级（脚底下方），符合用户“名称移到小人上面居中，经验值居中”的要求。

**修复内容：**
- 修复装备栏中小人模型过高、视觉上压迫上下文字与合成区的问题。
- 修复玩家昵称与经验等级并排显示在小人下方时，布局与概念稿“名称在上、等级在下”不符的问题。

---

## 2026-07-23

### 版本 1.3.8（修订）

**修改时间：** 2026-07-23 17:05

**提交内容：**
- 基于用户验收反馈，将 `LEFT_NAME_ABOVE_MODEL_BASE` 从 `6` 提升至 `24`，显著加大小人头顶与昵称之间的垂直间距，避免玩家名称被 3D 小人模型遮挡。
- 经验等级仍保持在小人脚底正下方居中显示。

**修复内容：**
- 修复 1.3.8 初版中玩家昵称被小人模型头部遮挡的问题。

---

## 2026-07-23

### 版本 1.3.8（二次修订）

**修改时间：** 2026-07-23 17:15

**提交内容：**
- 根据用户精确要求，将玩家昵称 Y 坐标固定为 `leftRowY(0) + 4`，即名称基线与头盔栏第一行槽位保持 4 像素的垂直对齐关系。
- `leftNameY()` 不再依赖模型顶部与 `LEFT_NAME_ABOVE_MODEL_BASE`，改为直接锚定到左侧护甲槽第一行（头盔槽）的 Y 坐标。

**修复内容：**
- 修复玩家昵称垂直位置不精确、可能被小人遮挡或离小人过远的问题，使其与头盔槽视觉对齐。

---

## 2026-07-23

### 版本 1.3.7

**修改时间：** 2026-07-23 13:30

**提交内容：**
- 将模组版本号从 `1.3.6` 迭代至 `1.3.7`。
- 继续根据反馈优化左侧面板装备栏：
  - 将 `LEFT_MODEL_GAP_BASE` 从 `130` 大幅提升至 `220`，2K 分辨率下模型与两侧槽位间距从约 199 像素增加至约 337 像素，给小人和槽位之间留出更宽松的呼吸空间。
  - 将 `LEFT_MODEL_SIZE_RATIO` 从 `0.25` 提升至 `0.4`，使原版 3D 小人在装备栏中更加醒目；在 2560×1440 分辨率下模型尺寸从约 84 像素提升至约 134 像素。
  - `computeLeftSlotSize()` 继续按 `(面板宽 - 模型间隙) / 2` 限制槽位尺寸，保证小人放大、间距加宽后各分辨率下仍不重叠。

**修复内容：**
- 修复 2K 分辨率下玩家模型与左右槽位间距仍然不足的问题。
- 修复装备栏中小人模型尺寸过小、不够醒目的的问题。

---

## 2026-07-23

### 版本 1.3.6

**修改时间：** 2026-07-23 13:20

**提交内容：**
- 将模组版本号从 `1.3.5` 迭代至 `1.3.6`。
- 继续加大 2K 分辨率下左侧面板装备栏水平间距：
  - 将 `LEFT_MODEL_GAP_BASE` 从 `70` 进一步提升至 `130`。
  - 在 2560×1440 分辨率下，模型与两侧槽位间距从约 107 像素提升至约 199 像素，确保小人不会被左右槽位夹住。
  - `computeLeftSlotSize()` 仍按 `(面板宽 - 模型间隙) / 2` 限制槽位尺寸，兼顾高分辨率间距需求与极小窗口下的防重叠保护。

**修复内容：**
- 修复 2K 分辨率下玩家模型与左右装备槽/护甲槽水平间距仍显不足的问题。

---

## 2026-07-23

### 版本 1.3.5

**修改时间：** 2026-07-23 13:10

**提交内容：**
- 将模组版本号从 `1.3.4` 迭代至 `1.3.5`。
- 优化 2K 等高分辨率下左侧面板装备栏布局：
  - 将 `LEFT_MODEL_GAP_BASE` 从 `30` 增大至 `70`，使原版 3D 小人与左右两列槽位（护甲槽、装备槽/副手槽）之间的水平间距随面板宽度同步放大。
  - 在 2560×1440 分辨率下，模型与两侧槽位间距从约 47 像素提升至约 107 像素，避免小人显得被槽位夹住。
  - `computeLeftSlotSize()` 的槽位尺寸上限改为按 `(面板宽 - 模型间隙) / 2` 计算，确保在极小面板下仍能为模型留出足够空间，防止两列槽位与模型重叠。

**修复内容：**
- 修复 2K 分辨率下玩家模型与左右装备槽/护甲槽水平间距过小、视觉上过于拥挤的问题。

---

## 2026-07-23

### 版本 1.3.4

**修改时间：** 2026-07-23 12:45

**提交内容：**
- 将模组版本号从 `1.3.3` 迭代至 `1.3.4`。
- 依据项目根目录 [`背包概念.txt`](file:///e:/project%20666/ModernizeGameFramework/背包概念.txt) 重新校准左侧面板装备栏布局：
  - 玩家模型垂直位置从顶部 4 行槽位区正中心下移至下半部分（约第 3-4 行之间），与概念稿中“原版 3D 小人”所处位置一致。
  - 玩家昵称/经验等级位置同步下移，紧贴模型底部下方显示。
  - 左下角 2×2 合成区与状态文本整体上移，不再锚定在面板最底部；通过计算名字/等级底部到面板底部的可用空间，将中部间距与底部留白按 2:8 分配，使合成区位于左侧面板中下位置，底部保留空白区域，符合概念稿布局。
  - 修复状态文本区高度按 4 行预留的问题，改为按实际渲染的 2 行（血量/饥饿/口渴一行、重量一行）计算，避免占用合成区上方空间。
- `TarkovInventoryMenu` 调整：
  - 新增 `LEFT_MODEL_VERTICAL_RATIO`（0.75）、`LEFT_MODEL_SIZE_RATIO`（0.25）、`LEFT_NAME_LEVEL_BELOW_MODEL_BASE`（4）常量。
  - 新增 `leftSlotBlockHeight()` / `leftModelSize()` / `leftModelCenterY()` / `leftNameLevelY()` 辅助方法，集中计算模型与文本位置。
  - `leftStatusBlockHeight()` 改为 `2 * lineHeight + 2`，与实际 2 行状态文本匹配。
  - `leftCraftBottomY()` 改为从顶部向下分配空间，正常分辨率下合成区位于中下位置；空间不足时回退到底部锚定。
  - 新增 `getLeftModelCenterY()` / `getLeftModelSize()` / `getLeftNameLevelY()` 访问器，供渲染层同步使用。
- `TarkovInventoryScreen` 调整：
  - 渲染玩家模型时改用 `menu.getLeftModelCenterY()` / `menu.getLeftModelSize()`，与菜单层计算保持一致。
  - 玩家昵称/经验等级渲染改用 `menu.getLeftNameLevelY()`，消除重复计算。

**修复内容：**
- 修复装备栏玩家模型位置偏高、未落在设计稿指定区域的问题。
- 修复合成区与状态文本贴底、导致名字/等级与合成区之间出现大面积空白的问题。
- 修复状态文本区高度预留过多（4 行）导致合成区被过度下推的问题。

---

## 2026-07-23

### 版本 1.3.3

**修改时间：** 2026-07-23 12:30

**提交内容：**
- 将模组版本号从 `1.3.2` 迭代至 `1.3.3`。
- 依据项目根目录 [`背包概念.txt`](file:///e:/project%20666/ModernizeGameFramework/背包概念.txt) 中的原定装备栏设计方案，重新校准左侧面板布局：
  - 左列护甲槽（头盔、胸甲、护腿、鞋子）与右列装备槽（胸挂、背包、安全箱）+ 副手槽保持两列排布。
  - 原版 3D 小人移至两列槽位之间，并与顶部 4 行槽位区垂直居中对齐，不再位于槽位区下方。
  - 玩家昵称与经验等级改为并排显示在小人正下方，符合概念图中 `<玩家昵称> <经验等级>` 的排列。
  - 左下角放置 2×2 合成格，结果槽位于材料右侧；在合成格右侧渲染“格子（合成栏）”标签。
  - 血量、饥饿、口渴、重量状态文本占位移到底部，按“第一行 3 项 / 第二行 1 项”排列在合成区下方。
- `TarkovInventoryMenu` 新增布局计算辅助：
  - 新增 `LEFT_MODEL_GAP_BASE`、`LEFT_STATUS_LINE_HEIGHT_BASE`、`LEFT_STATUS_GAP_BASE` 常量。
  - 新增 `leftModelGap()` / `leftSideMargin()`，按小人间距与槽位尺寸动态计算两列的两侧留白。
  - 新增 `leftStatusLineHeight()` / `leftStatusGap()` / `leftStatusBlockHeight()`，为底部状态文本预留空间。
  - `leftCraftBottomY()` 改为扣除底部留白与状态文本区后返回合成区底部，确保合成格与状态文本不重叠。
  - `addCraftingSlots()` 结果槽使用 `min(右列X, 材料右侧X)`，防止小面板下溢出。
  - 新增 `getLeftColX()` / `getRightColX()` / `getLeftTopPadding()` / `getLeftCraftBottomY()` / 状态相关 getter，供渲染层同步使用。
- `TarkovInventoryScreen` 同步调整渲染：
  - `renderPlayerModel()` 位置改为左侧面板水平中心、顶部槽位区垂直中心。
  - `renderPlayerNameAndLevel()` 改为接收单一 Y 坐标，昵称与等级并排绘制。
  - 新增合成栏标签渲染。
  - `renderStatusPlaceholders()` 改为两行排列（血量 / 饥饿 / 口渴、重量）。

**修复内容：**
- 修复装备栏布局与原定设计不符的问题：小人不再被挤到槽位区下方。
- 修复昵称/经验等级上下堆叠，与概念图并排显示不一致的问题。
- 修复合成区与状态文本可能重叠的问题。

---

## 2026-07-23

### 版本 1.3.2

**修改时间：** 2026-07-23 11:20

**提交内容：**
- 将模组版本号从 `1.3.1` 迭代至 `1.3.2`。
- 根据反馈优化左侧栏自适应缩放效果：
  - `TarkovInventoryMenu.computeLeftSlotSize()` 改为直接使用中部面板 `middleSlotSize` / `middleSlotGap`，使左侧护甲、装备、副手槽位与中部格子尺寸保持一致。
  - 新增 `LEFT_TOP_PADDING` / `LEFT_BOTTOM_PADDING` 常量，统一顶部装备区与底部合成区的留白基准。
  - 新增 `leftColX()` / `rightColX()` 方法，按左侧面板当前宽度计算两列槽位 X 坐标，使护甲列与装备列在面板内均匀分布。
  - 新增 `leftRowY(int row)` / `leftCraftBottomY()` 方法，行距由槽位尺寸与间隙动态决定，底部合成区按底部留白锚定。
  - `addEquipmentSlots()` / `addArmorSlots()` 改为使用动态计算的两列坐标；副手槽放置于右列第 4 行，与第 4 个护甲槽对齐。
  - `addCraftingSlots()` 按新的左侧面板缩放尺寸重新排布：2×2 材料槽位于左列，结果槽位于右列顶部。
- `TarkovInventoryScreen` 同步调整左侧渲染元素位置：
  - 根据顶部装备区 4 行高度与底部合成区 2 行高度，重新计算玩家模型垂直居中位置。
  - 玩家昵称上移至模型上方，经验等级下移至模型下方，间距按左侧面板宽度缩放。
  - 左下角状态条占位上移至合成区上方，行高与间距按左侧面板宽度缩放，避免与合成槽重叠。
- 同步更新 `ui_preview.html` 左侧栏预览：
  - 左侧槽位尺寸改为与中部格子一致。
  - 护甲、装备、副手、合成槽位按新的均匀分布与动态行距渲染。
  - 玩家模型占位位置与状态条占位位置同步调整。

**修复内容：**
- 修复左侧栏槽位尺寸与中部格子不一致导致视觉比例失调的问题。
- 修复左侧栏两列槽位固定坐标导致在小/大分辨率下分布不均的问题。
- 修复左侧栏槽位放大后与玩家模型、文字、状态条发生重叠的问题。
- 修复底部合成槽仍使用固定基准坐标，未随左侧槽位尺寸同步缩放的问题。

---

## 2026-07-23

### 版本 1.3.1

**修改时间：** 2026-07-23 10:20

**提交内容：**
- 将模组版本号从 `1.3.0` 迭代至 `1.3.1`。
- 实现左侧栏 UI 组件自适应缩放，与现有中部/右侧面板自缩放机制保持一致：
  - `TarkovInventoryMenu` 新增 `leftPanelRect`、`leftSlotSize`、`leftSlotGap` 字段，用于记录左侧面板矩形与槽位缩放后尺寸。
  - 新增 `LEFT_PANEL_BASE_WIDTH`（533）与 `LEFT_PANEL_BASE_HEIGHT`（860）常量，定义左侧面板内容区基准尺寸。
  - 新增基于左侧面板内容区的槽位基准坐标：`LEFT_ARMOR_X`/`LEFT_ARMOR_Y`、`LEFT_EQUIP_X`/`LEFT_EQUIP_Y`、`LEFT_OFFHAND_X`/`LEFT_OFFHAND_Y`、`LEFT_CRAFT_RESULT_X`/`LEFT_CRAFT_RESULT_Y`、`LEFT_CRAFT_MAT_X`/`LEFT_CRAFT_MAT_Y1`/`LEFT_CRAFT_MAT_Y2`。
  - 新增 `computeLeftSlotSize()` 方法：按左侧面板宽度等比计算槽位尺寸与间隙，保证不同分辨率下槽位与面板比例一致。
  - 新增 `leftPanelX()`、`leftPanelY()`、`leftPanelSize()` 辅助方法，将左侧面板内部基准坐标/尺寸转换为屏幕实际像素。
  - `addEquipmentSlots()`、`addArmorSlots()`、`addCraftingSlots()` 改为使用左侧面板相对坐标，使装备、护甲、副手、合成槽位随左侧面板自适应排布。
  - 新增 `isLeftPanelSlot()` 判断槽位是否属于左侧面板，新增 `getLeftSlotSize()` / `getLeftSlotGap()` / `getLeftPanelRect()` 访问器。
- `TarkovInventoryScreen` 同步左侧渲染元素自适应缩放：
  - `getSlotVisualSize()` 对左侧面板槽位返回 `menu.getLeftSlotSize()`，使槽位边框、物品图标、高亮框、锁定遮罩同步放大。
  - 玩家模型尺寸与位置改为基于左侧面板宽度缩放（`leftScale = leftPanel.width / 533`）。
  - 玩家昵称、经验等级、左下角状态条占位位置与行距均按左侧面板宽度缩放，保持相对位置一致。
- 同步更新 `ui_preview.html` 左侧栏预览：
  - 计算 `leftScale` 与 `leftSlotSize`，动态注入 `.left-slot` 尺寸样式。
  - 在左侧面板中渲染 4 个护甲槽、3 个装备槽和 1 个副手槽，位置按左侧面板内容区基准坐标等比映射。
  - 玩家模型占位尺寸与位置按左侧面板宽度自适应缩放。

**修复内容：**
- 修复左侧栏槽位与内部元素在高分辨率下未同步放大、导致视觉比例失调的问题。
- 修复左侧栏槽位坐标仍依赖整体背包面板缩放、未与左侧面板宽度直接关联的问题。

---

## 2026-07-23

### 版本 1.3.0

**修改时间：** 2026-07-23 00:48

**提交内容：**
- 将模组版本号从 `1.2.9` 迭代至 `1.3.0`。
- 修复 `TarkovInventoryScreen` 中多个与 Minecraft 1.20.1 原版方法签名不兼容导致的编译错误，使项目能够成功编译：
  - 重写 `isHovering(Slot, double, double)` 以让命中区域跟随放大后的槽位；由于原版 `isHovering(int,int,int,int,double,double)` 为 private，重写内部改为直接实现矩形命中测试，避免调用私有方法。
  - 1.20.1 中 `AbstractContainerScreen.renderSlotHighlight` 为 `private static`，无法被重写，因此移除错误的 `@Override` 尝试，改为在 `super.render()` 之后通过 `renderCustomSlotHighlight()` 在原版 16×16 高亮框之上补绘放大后的实际尺寸高亮框。
  - 1.20.1 中 `Screen.getBlitOffset()` 已不存在，自定义高亮框改用原版高亮框使用的 `HOVER_ITEM_BLIT_OFFSET`（200）作为 z 层级，确保覆盖原版高亮。
- 修正中部面板滚动偏移方向：`applyMiddleScrollOffset()` 中槽位 Y 坐标由 `+= offset` 改为 `-= offset`，使内容向下滚动时槽位向上移动，与 `UIScrollPanel` 的滚动方向一致。
- 修正提示框渲染时机：`renderTooltip()` 调整到恢复滚动偏移之前调用，避免提示框因槽位坐标被恢复而错位。
- 更新访问转换器配置 `src/main/resources/META-INF/accesstransformer.cfg`：
  - 新增 `AbstractContainerScreen.isHovering(Slot, double, double)` 为 `protected`，使其可被重写。
  - 移除对 `AbstractContainerScreen.renderSlotHighlight` 的访问转换（该方法为 static，转换访问级别也无法重写）。
- `ui_preview.html` 已完成中部面板严格 8 列、动态格子尺寸、滚动范围计算的预览实现，供手动验收；本次主要将对应逻辑在游戏内项目落实。
- 执行 `./gradlew compileJava` 成功，项目编译通过。

**修复内容：**
- 修复 `TarkovInventoryScreen` 因错误重写 `renderSlotHighlight` 和 `isHovering` 导致的编译失败。
- 修复 `TarkovInventoryScreen` 中 `getBlitOffset()` 方法不存在导致的编译失败。
- 修复中部面板滚动方向相反的问题。
- 修复滚动后物品提示框位置错位的问题。

---

## 2026-07-22

### 版本 1.2.9

**修改时间：** 2026-07-22 23:25

**提交内容：**
- 将模组版本号从 `1.2.8` 迭代至 `1.2.9`。
- 中部物品栏流式布局优化：
  - `TarkovInventoryMenu` 新增 `getMiddlePanelColumns()` 方法，根据中部面板实际可用宽度动态计算每行可显示的槽位列数。
  - 新增 `addFlowSlots()` 方法，将主仓库、扩展格、安全箱槽位按流式布局依次排列，格子自动换行，避免重叠。
  - 新增 `middlePanelColumns`、`nextMiddleY`、`middlePanelSlotStart`、`middlePanelSlotEnd`、`middleContentHeight` 等字段，记录中部面板布局状态。
  - `addMainInventorySlots()`、`addExpansionSlots()`、`addSecureSlots()` 均改为调用 `addFlowSlots()`，统一中部面板槽位生成逻辑。
- 中部物品栏滚动功能实现：
  - `TarkovInventoryScreen` 集成 `UIScrollPanel` 的滚动能力：渲染时通过 `applyMiddleScrollOffset()` 将中部面板槽位按滚动偏移整体上移。
  - 重写 `renderSlot()`，对超出中部面板边界的槽位进行裁剪，避免渲染到面板外部。
  - `renderSlotOutlines()` 与 `renderLockedOverlays()` 同样跳过越界槽位。
  - 鼠标点击、释放、拖拽事件位于中部面板区域时，通过 `dispatchMouseEventToMiddlePanel()` 临时应用滚动偏移，确保命中测试与可见槽位对齐。
- 滚动范围动态计算：
  - `TarkovInventoryMenu.computeMiddleContentHeight()` 以中部面板顶部为基准，计算到最低槽位底部的距离作为内容总高度。
  - `TarkovInventoryScreen.rebuildUI()` 中调用 `middleScrollPanel.setContentHeight()` 设置滚动范围；内容未超出面板高度时自动隐藏滚动条。
- 访问转换器配置：
  - 新增/修正 `src/main/resources/META-INF/accesstransformer.cfg`：移除 `Slot.x` / `Slot.y` 的 `final` 修饰符，并将 `AbstractContainerScreen.renderSlot()` 方法访问级别改为 `protected`，以支持滚动偏移修改与重写裁剪。
  - `build.gradle` 取消 `accessTransformer` 配置注释，使访问转换规则生效。
- 同步更新 `ui_preview.html`：中部面板改为流式布局，添加滚动条与鼠标滚轮交互演示，信息栏实时显示中部面板列数、内容高度、可视高度与可滚动状态。
- 优化 `ui_preview.html` 格子尺寸：
  - 中部面板流式布局改为 **严格固定 8 列**，不再随窗口宽度变化列数，确保内容达到 8 格长度时触发自动换行。
  - 格子尺寸改为根据中部面板可用宽度动态计算：`slotSize = max(16, floor(可用宽度 / 8.875))`，使 800×600、1366×768、1920×1080、2560×1440 等不同分辨率下格子自动缩放，高分辨率下显示更大。
  - 间隙按格子尺寸的约 1/8 动态计算，并做边界保护防止溢出，保证布局美观。
  - 动态注入 `<style>` 覆盖 `.slot` 与 `.hotbar-slot` 尺寸，使右侧容器区、底部快捷栏槽位随分辨率同步缩放；快捷栏槽位尺寸受底边栏高度限制，避免超出底边栏。
  - 右侧容器区占位网格改为 `repeat(9, ${slotSize}px)`，间隙同步使用 `slotGap`。
  - `renderFlowSection()` 改为根据传入的 `slotSize` / `slotGap` 动态计算容器宽度与间隙。
- 执行 `./gradlew runClient` 启动客户端并进入世界，未出现崩溃或 `Slot not in valid range` 异常。

**修复内容：**
- 修复中部物品栏格子可能重叠或超出面板边界的问题：改为根据面板宽度动态计算列数并自动换行。
- 修复中部物品栏内容超出面板时无法滚动浏览的问题。
- 修复滚动范围计算未考虑面板顶部留空区域，导致最下方格子无法完全滚动显示的问题。

---

## 2026-07-22

### 版本 1.2.8

**修改时间：** 2026-07-22 21:15

**提交内容：**
- 将模组版本号从 `1.2.7` 迭代至 `1.2.8`。
- 修复 `TarkovInventoryScreen` 编译错误：为 `UILayout` 补充 `scaled()`、`slotStep()`、`leftPanel()`、`middlePanel()`、`rightPanel()` 以及 `MARGIN`、`TOP_BAR_HEIGHT`、`SECTION_GAP` 等缺失的方法与常量，使屏幕渲染逻辑与背包菜单的响应式坐标系统保持一致。
- 统一底边栏高度计算：`TarkovInventoryScreen.rebuildUI()` 中底边栏高度改为调用 `UILayout.bottomBar(width, height).height()`，确保其严格等于窗口高度的 8% 并限制在 40~60 像素之间。
- 优化 `UIEventScheduler.debounce` 实现：
  - 使用全局单线程调度器（`ScheduledExecutorService`）管理延迟任务，避免旧实现中每次调用都创建新线程的问题。
  - 新增 `DebouncedAction<T>` 接口，支持 `cancel()` 方法取消 pending 任务。
  - 补充实现原理注释，说明 throttle 与 debounce 的算法差异。
- 在 `TarkovInventoryScreen` 中集成 resize 防抖重建：
  - 添加 `RESIZE_DEBOUNCE_MS = 150` 常量与 `debouncedResize` 字段。
  - 窗口尺寸变化时不再立即重建界面，而是通过 `UIEventScheduler.debounce` 延迟 150ms 执行，避免拖拽窗口时频繁刷新。
  - 防抖回调通过 `minecraft.execute()` 切换回主线程执行 `rebuildForResize()`，创建新的 `TarkovInventoryMenu` 与 `TarkovInventoryScreen` 完成同步重建。
  - 重写 `onClose()`，在关闭界面时取消 pending 的 resize 重建任务，防止界面关闭后仍尝试重建旧屏幕。
- 添加 JUnit 5 测试框架：
  - 在 `build.gradle` 中新增 `junit-jupiter-api`、`junit-jupiter-params` 与 `junit-jupiter-engine` 依赖。
  - 配置 `test { useJUnitPlatform() }` 使用 JUnit Platform 运行测试。
- 编写响应式布局单元测试：
  - `UIUnitTest`：验证 `vw`、`vh`、`rem`、`em`、`percent`、`clamp(int)`、`clamp(float)` 的正确性。
  - `UIScaleTest`：验证 `byHeight`、`byWidth`、`byMinSide` 的缩放系数计算、上下限约束以及 `px`、`pxByMinSide` 像素转换。
  - `UILayoutTest`：验证 1366×768、1920×1080、2560×1440 分辨率下背包面板保持 16:9、水平居中、底边栏高度 clamp、子面板位置关系、快捷栏居中以及坐标/尺寸映射。
  - `UIEventSchedulerTest`：验证 throttle 在间隔内只执行第一次、debounce 连续调用只执行最后一次、取消后不再执行、间隔足够时各执行一次。
- 执行 `./gradlew test` 成功，全部单元测试通过。
- 执行 `./gradlew build` 成功构建，输出新版本 JAR 到 `build\libs\modernizegameframework-1.2.8.jar`。
- 执行 `./gradlew runClient` 成功启动客户端并进入世界，未出现崩溃或异常。
- 新增 `ui_preview.html`：使用 HTML/CSS/JavaScript 可视化当前背包 UI 的响应式布局，支持 1366×768、1920×1080、2560×1440、800×600、4K 等预设分辨率，并可通过拖拽右下角实时调整窗口尺寸，直观观察 16:9 面板、底边栏和内部区域的同步缩放效果。
- 调整 UI 层级：`TarkovInventoryScreen.rebuildUI()` 中将 `topBar` 最后加入 `uiComponents`，确保渲染顺序在背包面板、左右中区域之上，实现顶部栏覆盖所有 UI 的效果；鼠标事件从后向前遍历，顶部栏也会优先消费点击事件。
- 同步更新 `ui_preview.html`：为 `.top-bar`、`.bottom-bar` 设置 `z-index: 100`，为 `.backpack-panel` 设置 `z-index: 1`，使预览中的顶部栏和底边栏也覆盖在背包面板上方。
- 重新划分三段式面板比例：
  - `UILayout` 中将背包面板横向均分为三段，每段基准宽度约为 533，子面板宽高比约为 16:27。
  - `TarkovInventoryMenu` 同步更新 `MIDDLE_PANEL_X`、`RIGHT_PANEL_X` 以及主仓库、扩展格、安全箱、容器槽位的基准 X 坐标，使各区域槽位落入对应面板，避免重叠。
  - `ui_preview.html` 同步改为三等分布局，并补充右侧容器槽位占位。
- 补充 `UILayoutTest` 子面板 16:27 宽高比单元测试，验证 1366×768、1920×1080、2560×1440 分辨率下左/中/右三个面板均保持该比例。
- 重新执行 `./gradlew test`、`./gradlew build` 均成功；启动客户端验证无崩溃。

**修复内容：**
- 修复 `UILayout` 缺少 `TarkovInventoryScreen` 所需方法与常量导致的编译失败。
- 修复底边栏高度未按规范限制在 40~60 像素的问题。
- 修复 `UIEventScheduler.debounce` 高频调用时创建大量线程的性能隐患。
- 修复窗口尺寸变化时背包界面可能频繁重建的问题，改为防抖延迟重建。

---

## 2026-07-22

### 版本 1.2.7

**修改时间：** 2026-07-22 23:15

**提交内容：**
- 将模组版本号从 `1.2.6` 迭代至 `1.2.7`。
- 新增自研 UI 组件库 `com.modernizegameframework.ui`：
  - `UIComponent`：所有 UI 组件的抽象基类，统一位置、尺寸、可见性、启用状态及鼠标事件接口。
  - `UIPanel`：面板容器，可嵌套子组件，支持背景色、边框、内边距和事件冒泡。
  - `UIScrollPanel`：可垂直滚动的面板，内置剪刀裁剪、滚动条渲染、鼠标滚轮与拖拽滚动支持。
  - `UITabBar`：横向标签栏组件，支持选中状态与点击回调。
  - `UIBlurBackground`：毛玻璃背景工具，提供全屏/区域半透明灰黑叠加层。
  - `UILayout`：屏幕锚定布局工具，固定计算顶部/底部边栏、左侧装备区、中部主仓库区、右侧容器区的矩形，确保不同分辨率下界面结构一致。
- 使用自研 UI 组件库重构 `TarkovInventoryScreen`：
  - 全屏毛玻璃背景替代原来的纯色背景。
  - 顶部边栏使用 `UITabBar` 显示装备/医疗/技能标签。
  - 底部边栏作为快捷栏背景，横跨屏幕底部。
  - 左侧装备区、中部主仓库区、右侧容器区均使用 `UIPanel`/`UIScrollPanel` 绘制背景与边框。
  - 鼠标事件优先经过 UI 组件树，再交给原版的槽位交互。
- 调整 `TarkovInventoryMenu` 槽位坐标以配合新布局：
  - 中部主仓库、扩展格、安全箱统一左对齐到 `MIDDLE_START_X = 104`。
  - 右侧容器槽位从 `RIGHT_START_X + SECTION_PADDING = 282` 开始，与右侧面板对齐。
  - 快捷栏锚定底部边栏中央，客户端根据当前窗口高度计算 y 坐标，服务端使用默认值。
- 执行 `./gradlew build` 成功构建，输出新版本 JAR 到 `build\libs\modernizegameframework-1.2.7.jar`。
- 修复快捷栏未居中对齐底部边栏的问题：`TarkovInventoryMenu.addHotbarSlots()` 中快捷栏 x 坐标改为 `(screenWidth - hotbarWidth) / 2`，y 坐标改为底部边栏垂直居中，使其始终位于底部边栏内。
- 实现 UI 自适应缩放：
  - `UILayout` 新增 `scale()`/`scaled()` 方法，以 480 高度为基准，所有布局尺寸按屏幕高度比例缩放（限制在 0.5~2.0 倍）。
  - `TarkovInventoryMenu` 引入 `screenWidth`/`screenHeight`/`slotSize`/`slotGap` 等缩放后字段，所有槽位坐标（装备、护甲、合成、主仓库、扩展格、安全箱、容器、快捷栏）均按缩放比例计算。
  - `TarkovInventoryScreen` 中槽位边框、锁定遮罩、玩家模型位置、状态条位置、安全箱标签位置均改为缩放后计算，确保与槽位实际位置一致。
- 执行 `./gradlew build` 成功构建并生成 `build\libs\modernizegameframework-1.2.7.jar`。

**修复内容：**
- 背包界面全屏锚定，不同分辨率下保持一致的布局结构。
- 界面视觉精致化：毛玻璃背景、统一面板边框、顶部/底部边栏。
- 快捷栏正确显示在底部边栏内。
- UI 元素随屏幕分辨率自适应缩放。
- 修正自适应缩放导致的槽位与物品图标错位：物品槽大小固定为 16×16（与原版物品图标一致），仅缩放槽位位置与间距，确保 UI 面板、格子边框、物品图标三者位置对齐。
- 实现窗口尺寸变化时自动重建背包界面：`TarkovInventoryScreen.resize()` 中检测到当前尺寸与菜单创建时不同，会新建 `TarkovInventoryMenu` 和 `TarkovInventoryScreen` 并切换，保证背景面板与槽位坐标始终同步；关闭并重新打开背包也会重建菜单实现同步。

---

## 2026-07-22

### 版本 1.2.6

**修改时间：** 2026-07-22 22:45

**提交内容：**
- 将模组版本号从 `1.2.5` 迭代至 `1.2.6`。
- 移除旧安全箱创造标签页：
  - `SecureContainerRegistry` 中删除 `CREATIVE_TABS` 与 `SECURE_CONTAINER_TAB` 注册，避免与塔科夫自定义装备栏重复显示安全箱。
  - `ModernizeGameFramework` 主类中移除 `SecureContainerRegistry.CREATIVE_TABS.register(modEventBus);` 调用。
- 自定义装备栏图标改为阿尔法安全箱图标：`TarkovInventoryRegistry.EQUIPMENT_TAB` 的 `icon` 从 `CHEST_RIG_LEVEL_6` 改为 `SecureContainerRegistry.ALPHA`。
- 安全箱内容格左对齐扩展背包格：`TarkovInventoryMenu.addSecureSlots()` 中 `startX` 从居中计算改为 `LEFT_WIDTH + SECTION_PADDING`，与扩展背包格保持同一左边界，`y` 坐标不变。
- 修复装备安全箱后刷新闪屏/闪退问题：删除 `TarkovInventoryScreen` 中通过 `lastSecureType` 检测安全箱变更并关闭/重新打开背包的逻辑，避免界面重建导致的闪烁和崩溃。
- 实现安全箱装备/卸下/更换后的界面实时刷新（服务端延迟重建方案）：
  - `TarkovInventoryMenu` 新增 `lastSecureType` 字段，在构造函数中记录当前安全箱类型。
  - `slotsChanged` 中检测到装备槽安全箱类型变化时，调用 `TarkovInventoryEvents.markForRebuild()` 标记该玩家需要重建界面。
  - `TarkovInventoryEvents.ServerEvents` 新增 `onServerTick` 事件，在服务端 tick 结束时统一处理重建请求：对仍打开塔科夫背包的玩家调用 `NetworkHooks.openScreen()` 重新打开界面，延迟一 tick 避免在 slot 操作过程中切换菜单。
  - 重建时保留当前右侧外部容器信息（容器实例与标题），确保打开箱子时装备安全箱不会丢失容器面板。
- 执行 `./gradlew build` 成功构建，输出新版本 JAR 到 `build\libs\modernizegameframework-1.2.6.jar`。
- 执行 `./gradlew runClient` 启动客户端，游戏正常加载并进入世界，未出现崩溃或 `Slot not in valid range` 异常。

**修复内容：**
- 修复创造模式物品栏中安全箱标签页与自定义装备栏重复的问题。
- 修复装备安全箱后界面刷新导致的闪屏/闪退问题。
- 修复删除旧刷新逻辑后安全箱装备/卸下/更换不实时刷新的问题。

---

## 2026-07-22

### 版本 1.2.5

**修改时间：** 2026-07-22 14:25

**提交内容：**
- 在 `TarkovInventoryScreen` 中实现安全箱装备/卸下/更换后实时刷新背包界面：
  - 新增 `lastSecureType` 字段记录当前菜单创建时的安全箱类型。
  - 新增 `getCurrentSecureType()` 辅助方法获取玩家当前装备的安全箱类型。
  - 在 `render()` 开头比较当前类型与记录类型，不一致时关闭当前菜单并发送 `OpenInventoryPacket` 请求服务端重新打开，以重建槽位布局。
- 执行 `./gradlew build` 成功构建，`./gradlew runClient` 成功进入世界，无崩溃。

**修复内容：**
- 修复安全箱在新布局中不是实时显示的问题：装备、卸下或更换安全箱后无需手动开关背包即可自动刷新界面。

---

## 2026-07-22

### 版本 1.2.5

**修改时间：** 2026-07-22 04:45

**提交内容：**
- 将模组版本号从 `1.2.4` 迭代至 `1.2.5`。
- 将 5 种安全箱（Alpha/Beta/Gamma/Kappa/Theta）焊入塔科夫背包界面：
  - `TarkovInventoryCapability.MAX_SECURE_SLOTS` 从 4 格扩容至 12 格（兼容 Kappa 3×4），新增 `getSecureCaseType()` 默认方法。
  - `TarkovInventoryCapabilityImpl.secureInventory` 同步扩容至 12 格。
  - `TarkovInventoryMenu.addSecureSlots()` 根据装备的 `SecureContainerType` 动态生成 `cols × rows` 格子，在中部面板居中排列，装备安全箱时显示对应行列内容格。
  - `TarkovInventoryMenu.SecureSlot` 新增禁止放入 `SecureContainerItem` 的检查（安全箱不能放入安全箱）。
  - `TarkovInventoryScreen` 安全箱标签改为居中显示，使用翻译键显示类型名称（如 "Alpha 安全箱"）。
  - `mfg:secure_case` 标签更新为包含 5 种 `SecureContainerItem`。
  - `TarkovInventoryRegistry` 移除旧的 `secure_case` 通用物品，创造标签页改为引用 `SecureContainerRegistry` 的 5 种安全箱。
- 修复左侧装备栏布局：调整顺序为背包(70,57) → 安全箱(70,79) → 副手(70,101)，不再重叠。
- 新增 `NotifyingSlotItemHandler` 内部类：装备槽变更时自动调用 `TarkovInventoryHelper.rebalanceItems()`，卸下/更换胸挂或背包后将锁定物品转移到可用格子或掉落。
- shift+左键快速移动排除副手槽：`quickMoveStack` 的 `equipmentEnd` 改为 `OFFHAND_INDEX`，防止物品优先进入副手。
- 快捷栏 Y 位置改为动态计算：紧跟安全箱区域下方（`secureEndY + 8`），移除固定的 `BASE_HEIGHT` 常量。
- 执行 `./gradlew build` 成功构建，输出新版本 JAR 到 `build\libs\modernizegameframework-1.2.5.jar`。

**修复内容：**
- 修复旧存档 `TarkovInventoryCapabilityImpl.deserializeNBT` 中 `secureInventory.deserializeNBT()` 读取旧存档 `Size: 4` 字段将 handler 缩回 4 格导致 `Slot 4 not in valid range` 崩溃的问题：改为手动逐条读取 `Items` 列表，忽略旧 `Size` 字段。
- 修复 `SecureContainerEvents.onPlayerClone` 同步时机问题：死亡重生后安全箱 overlay 不显示，添加 `SecureContainerNetwork.syncAll()` 调用。
- 修复安全箱无法放入 Curios 自定义槽位：新增 `data/curios/tags/items/secure_container.json` 标签文件，包含全部 5 种安全箱。

---

## 2026-07-22

### 版本 1.2.4

**修改时间：** 2026-07-22 04:12

**提交内容：**
- 将模组版本号从 `1.2.3` 迭代至 `1.2.4`。
- 完成 `./gradlew build` 构建并运行客户端验证：游戏正常加载，玩家可进入世界，未出现崩溃或异常。
- 实现塔科夫背包全屏布局：`TarkovInventoryScreen.init` 将 `imageWidth` / `imageHeight` 设为屏幕尺寸，并固定 `leftPos` / `topPos` 为 0，使界面横向占满整个 Minecraft 窗口，保留左侧装备区、中段主仓库/扩展格、右侧容器区的三段式结构。
- 实现安全箱功能：
  - `TarkovInventoryCapability` 接口新增 `SLOT_SECURE_CASE`、`MAX_SECURE_SLOTS`、`getSecureCase` / `setSecureCase` / `getSecureInventory`。
  - `TarkovInventoryCapabilityImpl` 扩展装备槽至 3 个，新增 2×2 安全箱内容格，序列化/反序列化时保存 `Secure` 数据。
  - `TarkovInventoryMenu` 在装备安全箱时添加 4 个 `SecureSlot`，未装备时不添加内容格（直接隐藏）。
  - `TarkovInventoryMenu` 新增 `hasSecureSlots` 标志，修正未装备时 `isSecureSlot` / `isLockedSlot` 对主仓库前 4 格的误判定。
  - `TarkovInventoryCapabilityRegistry` 在玩家死亡/克隆时保留安全箱装备槽与内容格，仅扩展格物品通过 `LivingDropsEvent` 掉落。
  - `TarkovInventoryRegistry` 注册 `secure_case` 物品并加入装备创造标签页。
  - 新增 `data/mfg/tags/items/secure_case.json` 标签文件，限制只有安全箱物品可放入安全箱装备槽。
- 简化 `OpenInventoryPacket` 为无参包，修复 `TarkovInventoryEvents.onGuiOpen` 空参调用导致的编译错误；`TarkovInventoryMenuProvider` 新增 `extraDataWriter` 静态方法供 `NetworkHooks.openScreen` 使用。
- 执行 `./gradlew build` 成功构建，输出新版本 JAR 到 `build\libs\modernizegameframework-1.2.4.jar`。

**修复内容：**
- 修复 `OpenInventoryPacket` 构造函数与调用方参数不匹配导致的编译失败。
- 修复 `TarkovInventoryScreen` 中直接修改 `Slot.x` / `Slot.y` 失败的问题（`Slot` 字段为 final），改为在 `TarkovInventoryMenu` 中根据安全箱装备状态动态决定是否添加槽位，实现隐藏效果。
- 修复按 E 无法打开背包的问题：旧存档反序列化后 `Equipment` ItemStackHandler 只有 2 个槽位（胸挂/背包），访问新增的安全箱槽位 2 时越界。`TarkovInventoryCapabilityImpl.deserializeNBT` 改为手动读取装备槽数据，兼容旧存档并在读取前清空为 3 个槽位。
- 将 `ModernizeGameFramework.LOGGER` 访问权限改为 `public`，便于各模块统一记录日志。

---

## 2026-07-22

### 版本 1.2.3

**修改时间：** 2026-07-22 00:12

**提交内容：**
- 将模组版本号从 `1.2.2` 迭代至 `1.2.3`。
- 在 `TarkovInventoryScreen` 新增 `renderSlotOutlines` 方法，为每个物品槽绘制自定义灰色边框线。

**修复内容：**
- 修复塔科夫背包快捷键（Alt+点击移到装备区、Ctrl+点击移到容器区）移动物品后客户端显示位置错乱的问题：在 `TarkovInventoryQuickMove.moveToEquipment` 和 `moveToContainer` 末尾调用 `menu.broadcastChanges()`，确保服务端修改后立即同步到客户端。

---

## 2026-07-21

### 版本 1.2.2

**修改时间：** 2026-07-21 21:10

**提交内容：**
- 将模组版本号从 `1.2.1` 迭代至 `1.2.2`。
- 新增 `TarkovInventoryScreen` 自定义三段式背包界面，包含顶部标签页（装备/医疗/技能，后两者点击提示尚未开放）、左侧装备区、中段主仓库/扩展格、右侧容器区（打开箱子时显示）。
- 新增 `TarkovInventoryEvents` 事件监听器，客户端通过 `ScreenEvent.Opening` 拦截原版 `InventoryScreen`，服务端通过 `PlayerInteractEvent.RightClickBlock` 拦截容器方块打开并替换为塔科夫背包界面。
- 新增 `TarkovInventoryNetwork` 网络通道，实现客户端请求打开背包、装备槽与扩展格数据同步、Alt/Ctrl 快捷键移动网络包。
- 新增 `TarkovInventoryQuickMove` 服务端快捷键移动逻辑：Alt+点击移到装备区（胸挂/背包/护甲/副手），Ctrl+点击移到右侧容器区。
- 在 `ModernizeGameFramework` 主类中注册塔科夫背包的物品、菜单类型、创造标签页、网络消息与客户端 Screen。
- 为 `TarkovInventoryCapability` 接口及实现补充 `getEquipmentInventory()` 方法，供装备槽读取。
- 更新 `zh_cn.json` / `en_us.json` 语言文件，新增标签页“尚未开放”提示文本。

**修复内容：**
- 修复 `TarkovInventoryMenu` 中 `TransientCraftingContainer` 错误使用玩家背包列表的问题，改为独立 2x2 合成格列表，避免污染玩家背包。
- 修复 `TarkovInventoryHelper.tryPlaceInOpenSlots` 中 lambda 引用非最终局部变量导致的编译错误。
- 修复 `TarkovInventoryScreen` 中错误重写私有 `renderSlot` 方法的问题，改为在 `render` 流程结束后统一绘制锁定格遮罩。

---

## 2026-07-21

### 版本 1.2.2

**修改时间：** 2026-07-21 23:45

**提交内容：**
- 无新增功能。

**修复内容：**
- 修复创造模式背包被错误替换为塔科夫背包的问题：在 `TarkovInventoryEvents.onGuiOpen` 中排除 `CreativeModeInventoryScreen`，仅替换生存模式原版背包。

---

## 2026-07-21

### 版本 1.2.2

**修改时间：** 2026-07-21 23:55

**提交内容：**
- 无新增功能。

**修复内容：**
- 进一步加强创造模式背包保护：在 `TarkovInventoryEvents.onGuiOpen` 中增加玩家 `isCreative()` 判断，确保创造模式下任何背包界面都不会被替换为塔科夫背包。

---

## 2026-07-21

### 版本 1.2.1

**提交内容：**
- 将模组版本号从 `1.2.0` 迭代至 `1.2.1`，避免覆盖已存在的构建产物。
- 执行 `./gradlew build` 成功构建，输出新版本 JAR 到 `build\libs\modernizegameframework-1.2.1.jar`。

**修复内容：**
- 无（本次为版本输出，未修复具体功能问题）。

---
